Atualize a API principal AutoPonto para que `GET /api/edge/pull/?node_id=<NODE_ID>`, autenticado por `Authorization: NodeToken <token>`, retorne um snapshot autoritativo do dia no formato `{"snapshot_data":"YYYY-MM-DD","synced_at":"ISO-8601","cache_redis":{...}}`, sem `data`, entidades completas, cursores, incremental ou `deleted`; `cache_redis` deve vir pronto para o edge gravar no Redis com `dispositivos_por_codigo` por MAC/codigo contendo `dispositivo_id`, `dispositivo_codigo`, `sala_id`, `ativo` e `interscity_uuid`, `aulas_por_sala` como listas ordenadas de aulas validas do dia com `id`, `nome`, `turma_id`, `sala_id`, `inicio`, `fim` e `status`, `alunos_por_aula` como mapa `aula_id -> [aluno_id]`, `alunos_por_id` com pelo menos `nome`, e `embeddings_faciais` por `embedding_id` contendo `alunoId` e `embedding` no formato `{"dtype":"float32","shape":[1,N],"data":[...]}` esperado pelo face-worker; o backend deve continuar recebendo `POST /api/edge/attendance/` com `node_id` e eventos de presenca (`id`, `aluno_id`, `aula_id`, `dispositivo_id`, `reconhecido_em`, `score`) e responder `synced_ids`.
